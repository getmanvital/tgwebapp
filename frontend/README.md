# Frontend (Telegram Web App)

React/Vite приложение, запускаемое внутри Telegram Web Apps.

## Возможности

- Просмотр подборок (альбомов VK) на главной.
- Каталог товаров с поиском и фильтром по размеру.
- Кнопка `MainButton` Telegram для связи с менеджером.

## Технологии

- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик
- **Tailwind CSS** - CSS фреймворк (см. [STYLE_GUIDE.md](./STYLE_GUIDE.md))

## Быстрый старт

```bash
cd frontend
npm install
npm run dev
```

Сконфигурируйте `.env` (см. `.env.example`) с URL backend.

## Структура

```
src/
  components/    // UI-компоненты (карточки, фильтры)
  pages/         // Страницы Web App
  hooks/         // Telegram/Web API хуки
  services/      // Клиенты API
  types/         // Типы данных
  styles/        // Глобальные стили (Tailwind)
```

## Стилизация

**ВАЖНО:** Проект использует **Tailwind CSS** как единый CSS фреймворк.

📖 **См. [STYLE_GUIDE.md](./STYLE_GUIDE.md)** для правил написания стилей.

### Основные правила:
- ✅ Использовать Tailwind utility классы
- ✅ Использовать Telegram цвета (`tg-*`)
- ❌ Не использовать inline стили
- ❌ Не создавать кастомные CSS классы



















