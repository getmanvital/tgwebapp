# Telegram Web App Showcase

Витрина товаров для Telegram Web App с интеграцией каталога из VK `market`.

## Структура проекта

```
.
├── backend/           # Сервис-прокси для VK API, кэш и нормализация данных
├── frontend/          # Telegram Web App (SPA)
├── docs/              # Документация и схемы
└── .gitignore
```

## Основные модули

- `frontend` — клиентское приложение (React + Telegram Web Apps SDK) с каталогом, поиском и фильтром размеров.
- `backend` — Node/Express-прокси для запросов к VK, парсинг размеров, кэширование и единый REST API.
- `docs` — архитектурные заметки, API контракты, чек-листы тестирования.

## Быстрый старт

### Автоматическая настройка (Windows)

```powershell
.\scripts\quick-deploy.ps1
```

### Ручная настройка

1. Установите зависимости отдельно для `frontend` и `backend`.
2. Заполните `.env` файлы (токен VK, ID группы, URL backend).
3. Запустите backend, затем frontend.
4. Зарегистрируйте URL фронтенда в BotFather для Web App.

**Подробности по модулям:** Смотрите README каждого пакета.

## Деплой на сервер

Для деплоя проекта на сервер и настройки Telegram WebApp смотрите:

- **[Деплой на Render.com](docs/DEPLOY_RENDER.md)** 🚀 — пошаговая инструкция для деплоя на бесплатный хостинг Render.com
- **[Шпаргалка по деплою](docs/DEPLOY_CHEATSHEET.md)** ⚡ — краткая инструкция для быстрого старта
- **[Полная инструкция по деплою](docs/DEPLOYMENT.md)** — подробное руководство по деплою на VPS с Nginx, SSL и PM2
- **[Быстрый деплой для тестирования](docs/QUICK_DEPLOY.md)** — упрощенная инструкция для быстрого тестирования, включая варианты с ngrok и бесплатными хостингами

### Полезные скрипты:

```bash
# Проверка готовности к деплою
bash scripts/check-deploy.sh

# Быстрая настройка .env файлов
bash scripts/setup-env.sh
```

### Краткая инструкция:

1. **Backend:** Соберите проект (`npm run build`), настройте `.env`, запустите через PM2
2. **Frontend:** Соберите проект (`npm run build`), настройте Nginx для раздачи статики
3. **SSL:** Настройте HTTPS (обязательно для Telegram WebApp) через Let's Encrypt
4. **Telegram:** Создайте бота через BotFather, настройте WebApp с URL вашего frontend
5. **Webhook:** Настройте webhook для получения уведомлений: `npm run webhook:setup`

## Telegram Webhook

### Автоматическая настройка ✨

Webhook настраивается **автоматически** при старте сервера! Просто убедитесь, что в `backend/.env` указаны:

```env
AUTO_SETUP_WEBHOOK=true
TELEGRAM_BOT_TOKEN=ваш_токен_от_botfather
TELEGRAM_MANAGER_ID=ваш_числовой_id
BACKEND_URL=https://ваш_домен.com
```

При деплое или перезапуске сервера webhook настроится сам.

### Ручная настройка (опционально)

Если нужно настроить вручную:

```bash
# Установка webhook
cd backend
npm run webhook:setup

# Проверка статуса
npm run webhook:info
```

Или используйте скрипты:
```bash
# Linux/Mac
bash scripts/post-deploy.sh

# Windows
.\scripts\post-deploy.ps1
```

**Требования:**
- `TELEGRAM_BOT_TOKEN` - токен от [@BotFather](https://t.me/botfather)
- `TELEGRAM_MANAGER_ID` - ваш числовой ID (получите у [@userinfobot](https://t.me/userinfobot))
- `BACKEND_URL` - публичный URL с HTTPS
- `AUTO_SETUP_WEBHOOK=true` - для автоматической настройки











